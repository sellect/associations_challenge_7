
<%= form_for(@note) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class='edit_form'>
    <div class="field">
      <%= f.label :message %><br>
      <%= f.text_area :message %>
    </div>
    <div class="field">
      <%= f.label :order_id %><br>
      <%= f.text_area :order_id %>
    </div>
    <div class="field">
      <%= f.label :customer_id %><br>
      <%= f.text_area :customer_id %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  </div>



<% end %>
